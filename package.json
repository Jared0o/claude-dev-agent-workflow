{
  "name": "dev-agent-workflow",
  "version": "0.1.0",
  "private": true,
  "description": "Claude Code plugin: risk-based development workflow with independent subagents and resumable local delivery.",
  "license": "MIT",
  "repository": "https://github.com/Jared0o/claude-dev-workflow",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "test": "node --test \"tests/**/*.test.mjs\"",
    "validate": "node scripts/validate.mjs",
    "check": "node --check hooks/guard.js && node --check hooks/session-reminder.js && node --check hooks/agent-log.js && node --check scripts/workflow.mjs && node --check scripts/validate.mjs",
    "ci": "npm run check && npm run validate && npm test"
  }
}
